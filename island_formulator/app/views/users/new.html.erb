<div class="max-w-md mx-auto mt-10 p-6 bg-white rounded-lg shadow-md">
  <h1 class="text-2xl font-bold mb-6 text-center text-green-900">Create Your Account 🌿</h1>

  <%= form_with model: @user, url: users_path, class: "space-y-4" do |form| %>
    <% if @user.errors.any? %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
        <ul class="list-disc list-inside">
          <% @user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= form.label :email_address, class: "block text-sm font-medium text-green-800 mb-1" %>
      <%= form.email_field :email_address, class: "w-full px-3 py-2 border border-green-700 rounded-md focus:outline-none focus:ring-2 focus:ring-green-800 bg-green-50", placeholder: "you@example.com", required: true %>
    </div>

    <div>
      <%= form.label :password, class: "block text-sm font-medium text-green-800 mb-1" %>
      <%= form.password_field :password, class: "w-full px-3 py-2 border border-green-700 rounded-md focus:outline-none focus:ring-2 focus:ring-green-800 bg-green-50", placeholder: "Choose a strong password", required: true %>
    </div>

    <div>
      <%= form.label :password_confirmation, "Confirm Password", class: "block text-sm font-medium text-green-800 mb-1" %>
      <%= form.password_field :password_confirmation, class: "w-full px-3 py-2 border border-green-700 rounded-md focus:outline-none focus:ring-2 focus:ring-green-800 bg-green-50", placeholder: "Type your password again", required: true %>
    </div>

    <%= form.submit "Create Account 🌿", class: "w-full bg-green-800 text-white py-2 px-4 rounded-md hover:bg-green-900 transition duration-200 font-medium" %>
  <% end %>

  <div class="mt-4 text-center text-sm text-gray-600">
    Already have an account? 
    <%= link_to "Log in", new_session_path, class: "text-green-800 hover:underline" %>
  </div>
</div>